apiVersion: v1
kind: Pod
metadata:
  name: network-tester
  namespace: nodejs
  labels:
    app: network-tester
spec:
  containers:
    - name: network-tester
      image: busybox
      command: ["sh", "-c", "sleep 3600"]
      resources: {}

